<h1><%= @student.name %></h1>

<h2>Courses</h2>

<% @student.courses.each do |course| %>
  <section id='course-<%= course.id %>'>
    <h4><%= course.name %></h4>
    <p>Grade: <%= @student.grade_in_course(course) %></p>
  </section>
<% end %>
